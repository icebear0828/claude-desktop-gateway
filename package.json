{
  "name": "claude-desktop-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin Claude Desktop inference gateway for OpenRouter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:real": "vitest run tests/real-openrouter.test.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4"
  }
}
